const fs = require('fs'); const env = fs.readFileSync('.env.local', 'utf8'); const url = env.match(/VITE_SUPABASE_URL=(.*)/)[1].trim(); const key = env.match(/VITE_SUPABASE_SERVICE_ROLE_KEY=(.*)/)[1].trim(); fetch(url + '/rest/v1/payment_transactions?select=response_payload,request_payload,error_message,success&order=created_at.desc&limit=1', {headers: {apikey: key, Authorization: 'Bearer ' + key}}).then(r=>r.json()).then(d=>console.log(JSON.stringify(d, null, 2)));
